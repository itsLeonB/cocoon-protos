version: v1

deps:
- buf.build/rleonb/audit

lint:
  use:
  - STANDARD
  ignore_only:
    RPC_RESPONSE_STANDARD_NAME:
    - auth/v1/auth_service.proto
    - friendship/v1/request_service.proto
    #   RPC_REQUEST_STANDARD_NAME:
    RPC_REQUEST_RESPONSE_UNIQUE:
    - auth/v1/auth_service.proto
    - friendship/v1/request_service.proto

breaking:
  use:
  - FILE
