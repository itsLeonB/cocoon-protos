syntax = "proto3";

package profile;

option go_package = "github.com/itsLeonB/cocoon-protos/gen/go/profile";

import "profile/profile_message.proto";

service ProfileService {
  rpc Get (ProfileRequest) returns (ProfileResponse);
  rpc Create (NewProfileRequest) returns (ProfileResponse);
}
